@model IEnumerable<Wiser.Models.Author.AuthorDetailItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WisdomCount)
        </th>
        <th>
            @Html.DisplayNameFor(model=> model.Virtue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName) @Html.DisplayFor(modelItem=> item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WisdomCount)
        </td>
        <td>
            @Html.DisplayFor(mdoelItem=> item.Virtue)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.AuthorId }) 
        </td>
    </tr>
}

</table>
